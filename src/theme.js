import { createTheme } from "@material-ui/core";

const theme = createTheme({
  palette: {
    primary: {
      main: "#161517",
    },
    secondary: {
      main: "#11B0C7",
    },
    info: {
      main: "#A93AE8",
    },
  },
});

export default theme;
